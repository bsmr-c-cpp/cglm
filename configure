#!/bin/sh

# easy makefile
echo '
.PHONY: all

all:
	make -C build all

%: 
	make -C build $@
' > Makefile

# cleanup build dir if exist
rm -rf build
mkdir  build && 

# create Unix Makefiles project
cd build && cmake ..

echo "\nYou can now run \"cd build && make all && make test ARGS=-V\"\n"

